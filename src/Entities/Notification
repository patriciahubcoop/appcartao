{
  "name": "Notification",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "T\u00edtulo da notifica\u00e7\u00e3o"
    },
    "message": {
      "type": "string",
      "description": "Mensagem"
    },
    "type": {
      "type": "string",
      "enum": [
        "transaction",
        "invoice",
        "security",
        "general"
      ],
      "description": "Tipo de notifica\u00e7\u00e3o"
    },
    "card_id": {
      "type": "string",
      "description": "ID do cart\u00e3o relacionado"
    },
    "is_read": {
      "type": "boolean",
      "default": false,
      "description": "Notifica\u00e7\u00e3o lida"
    },
    "priority": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ],
      "default": "medium"
    }
  },
  "required": [
    "title",
    "message",
    "type"
  ]
}